python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python process_document.py        # should produce data/processed/chunks.json
python create_embeddings.py       # will use fallback if sentence-transformers missing
